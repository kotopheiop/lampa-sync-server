# Lampa Sync Server

Пароль авторизации (Bearer token для плагина):
SYNC_PASSWORD=changeme

# Порт на хосте (внутри контейнера всегда 3000)
HOST_PORT=3000

# Каталог данных (в Docker: /app/data)
# DATA_DIR=./data
